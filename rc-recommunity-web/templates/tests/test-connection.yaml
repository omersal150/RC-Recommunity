apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rc-recommunity-web.fullname" . }}-test-connection"
  labels:
    {{- include "rc-recommunity-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rc-recommunity-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
